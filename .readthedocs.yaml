# Read the Docs configuration file
# See https://docs.readthedocs.io/en/stable/config-file/v2.html for details

# Required
version: 2

# Build environment
build:
  os: ubuntu-24.04
  tools:
    python: "3.12"   # If builds fail, try "3.12"

# Tell RTD where your Sphinx conf.py is
sphinx:
  configuration: conf.py   # Or docs/source/conf.py if you used separate source/build

python:
  install:
    - requirements: requirements.txt


formats:
  - pdf
  
